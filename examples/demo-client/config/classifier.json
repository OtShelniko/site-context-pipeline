{
  "_comment": "Optional override for the URL classifier. Rules are evaluated in order; the first match wins.",
  "rules": [
    { "page_type": "blog", "pattern": "*/blog/*" },
    { "page_type": "service", "pattern": "*/services/*" },
    { "page_type": "landing", "pattern": "*/pricing*" }
  ]
}
